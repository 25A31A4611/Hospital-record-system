# Hospital-record-system
The Hospital Record System is a simple C program used to manage patient details efficiently. It allows users to add, view, search, and delete patient records. The data is stored in a file, so it is saved permanently even after the program is closed.
